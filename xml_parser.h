#ifndef XML_PARSER_H
#define XML_PARSER_H

#include <vector>
#include <string>
#include "gtr_parser.h"

void convertXMLToGTR(const std::string& inputFileName, const std::string& outputFileName);

#endif